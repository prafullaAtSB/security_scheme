openapi: 3.0.0
info:
  title: SPS Security Domain
  description: Common components and schemas used for securing APIs.
  version: '1.0'
  termsOfService: https://developercenter.spscommerce.com/#/terms-of-use
  contact:
    name: SPS Commerce Support
    url: https://www.spscommerce.com/customer-support/support/
  x-sps-domain: Security
components:
  securitySchemes:
    SpsBearer:
      type: http
      scheme: bearer
      description: |-
        Bearer authentication specifies a bearer token in the 'Authorization' header following the format:
        Authorization: Bearer <token>
